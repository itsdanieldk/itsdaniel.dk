---
import type { CollectionEntry } from "astro:content";
import { readingTime } from "@lib/utils";
import Tag from "@components/Tag.astro";

type Props = {
  entry: CollectionEntry<"notes"> | CollectionEntry<"projects">;
};

const { entry } = Astro.props;
const showReadingTime = entry.collection === "notes";
---

<a
  href={`/${entry.collection}/${entry.id}/`}
  class="relative group flex py-4 px-5 pr-12 rounded-lg border border-black/15 dark:border-white/20 hover:bg-black/5 dark:hover:bg-white/5 hover:text-black dark:hover:text-white transition-colors duration-300 ease-in-out"
>
  <div class="flex flex-col flex-1">
    <div class="font-semibold text-lg">
      {entry.data.title}
    </div>
    <div class="text-base">
      {entry.data.description}
    </div>
    <div class="flex items-center gap-2 mt-1">
      {showReadingTime && <span class="text-sm text-black/40 dark:text-white/40">{readingTime(entry.body ?? "")}</span>}
      {
        entry.data.tags && entry.data.tags.length > 0 && (
          <div class="flex flex-wrap gap-1">
            {entry.data.tags.map((tag) => (
              <Tag tag={tag} />
            ))}
          </div>
        )
      }
    </div>
  </div>
  <svg
    xmlns="http://www.w3.org/2000/svg"
    viewBox="0 0 24 24"
    aria-hidden="true"
    class="absolute top-1/2 right-3 -translate-y-1/2 size-6 stroke-2 fill-none stroke-current"
  >
    <line
      x1="5"
      y1="12"
      x2="19"
      y2="12"
      class="translate-x-3 group-hover:translate-x-0 scale-x-0 group-hover:scale-x-100 transition-transform duration-300 ease-in-out"
    ></line>
    <polyline points="12 5 19 12 12 19" class="-translate-x-1 group-hover:translate-x-0 transition-transform duration-300 ease-in-out"
    ></polyline>
  </svg>
</a>
